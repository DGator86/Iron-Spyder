{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack enable && pnpm install --dir web --frozen-lockfile",
  "buildCommand": "pnpm --dir web build",
  "outputDirectory": "web/.next"
}
