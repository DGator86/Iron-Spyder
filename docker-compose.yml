services:
  api:
    build: .
    ports:
      - "8000:8000"
    command: uvicorn spy_der.app.api:app --host 0.0.0.0 --port 8000

  dashboard:
    build: .
    ports:
      - "8501:8501"
    command: streamlit run spy_der/app/dashboard.py --server.address 0.0.0.0 --server.port 8501
